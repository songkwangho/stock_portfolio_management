# attention 현저성 점수 재설계 — 지시문 (2026-08-12)

> 대상 파일: `server/domains/attention/score.js` (순수 모듈)
> 워크플로: 이 문서 → Claude Code 구현·커밋. DB/배포 불요(순수 계산 + 상수).
> ⚠️ **원문 미대조**: 이번 세션 GitHub 401 + 레포 미마운트로 `score.js` 소스를 직접 못 읽음.
> 아래 "현재 구조"는 **라이브 API 출력 역산 + CLAUDE.md 명문 상수**로 복원한 것(전부 실측 일치 검증).
> CC는 구현 전 실제 `score.js`와 대조해 컴포넌트 계산부(캡·decay·blend)가 아래와 일치하는지 먼저 확인할 것.

---

## 1. 문제 (라이브 관찰로 확정)

실계좌(보유 4종목) `/api/attention` 정렬:

| 순위 | 종목 | score | disc | move | unrl | stake | 상태 |
|---|---|---|---|---|---|---|---|
| 1 | 유유제약 | 0.0431 | 0.144 | 0.273 | 0.492 (**-14.75%**) | 1.0 (35%) | "기타" 공시 1건·12일 전 |
| 2 | 컴투스홀딩스 | 0.0245 | 0 | 0.161 | 1.0 (**-39.13%**) | 1.0 (**63%**) | 공시 없음 |
| 3 | 딥노이드 | 0.0064 | 0 | 0.437 | 1.0 (+33%) | 0.033 (1%) | 소액 |

**중요도 역전**: 사용자가 가장 확인해야 할 포지션(–39% 손실 + 비중 63%의 컴투스홀딩스)이
–14.75% + 12일 전 잡공시("기타"는 공시 카테고리 중 신호가치 최하)의 유유제약보다 **아래**로 밀림.

근본 원인은 disc 상수가 아니라 **집계 형태**다.
현재 `score = Π(0.1 + 0.9·c)`는 곱셈 = **논리곱(AND)/균형** 의미론:
"모든 축이 어느 정도 있어야 점수가 산다, 한 축이 낮으면 전체를 누른다." floor 0.1은 AND가
0으로 죽는 걸 막는 땜질이고, 그 땜질이 "컴포넌트 0→최대 = 10배 스윙"을 만든다.
그래서 단일 경미 공시(disc 0→0.144, 인자 0.1→0.23 ≈ 2.3배)가 심각한 손실을 이긴다.

**트리아지가 원하는 의미론은 정반대인 논리합(OR)**: 심하게 빠졌거나, **또는** 크게 움직였거나,
**또는** material 공시가 떴거나, **또는** 큰 덩어리면 — **어느 하나만 강해도 무조건 상단**.

> 주의: 이 OR 전환은 **attention 레이어에만** 적용한다. 7관점 균형 요약(quality 레이어)은
> 곱셈/균형이 철학적으로 맞다(한 축 독주 방지). 두 레이어는 집계 형태가 **반대**여야 한다.
> 이번 버그는 attention이 quality용 균형-곱셈을 잘못 빌려온 것.

---

## 2. 현재 구조 (복원·검증)

컴포넌트 `c ∈ [0,1]` (방향 배제, 크기만):

- `disc` = 공시 최신성×건수, `exp(-d/7)` 블렌드 · 14일 룩백 (`discDecayDays 7`, `discLookbackDays 14`)
- `move` = |5거래일 수익률|, **15% 캡** → 검증: 유유 4.1/15=0.273 ✓, 컴투스 2.41/15=0.161 ✓
- `unrl` = |미실현손익률|, **30% 캡** → 검증: 유유 14.75/30=0.492 ✓, 컴투스 39.13/30→1.0 ✓ (보유만; 관심 baseline 0.3)
- `stake` = 비중, **30% 캡** → 검증: 딥노이드 1/30=0.033 ✓, 35%·63%→1.0 ✓ (관심 baseline 0.3)

집계: `score = Π_{k}(0.1 + 0.9·c_k)` · `scoreFloor 0.006` 미달 컷 · 상위 K=5 · 보유·관심 dedupe(보유 우선).

---

## 3. 새 설계 — noisy-OR

```
score = 1 − Π_{k}(1 − w_k · c_k)
```

- 논리합: 어느 신호든 강하면 1로 밀림. [0,1] 경계 · 자연 캡.
- 컴포넌트 정의(캡·decay·baseline)는 **그대로 유지**. 바꾸는 건 집계부 + 가중치 `w_k` 신설.
- disc의 카테고리 가중·decay는 `c_disc` **안의 하위 디테일**로 흡수(§5).

### 가중치 초기값 (provisional — Phase 4 백테스팅 대상)

| 컴포넌트 | w | 근거 |
|---|---|---|
| `unrl` | 0.85 | 내 손익 — 단독으로도 상단 보장해야 하는 1순위 신호 |
| `stake` | 0.70 | 큰 덩어리는 단독으로도 주목 가치 |
| `move` | 0.45 | 시장 사건성, 중간 |
| `disc` | 0.25 | 신뢰도·노이즈 최다("기타" 다수). 정보는 주되 단독 지배 금지 |

의미: `c=1`인 단일 신호가 주는 최소 점수 = `w_k`. 즉 –30%↑ 손실(unrl=1)만으로 score≈0.85,
잡공시(disc≈0.14)만으로는 score≈0.035에 그침.

### 실데이터 검증 (계산 완료)

| 종목 | OLD | NEW(nOR) |
|---|---|---|
| 컴투스홀딩스 | 0.0245 (2위) | **0.958 (1위)** |
| 딥노이드 | 0.0064 (3위) | 0.882 (2위) |
| 유유제약 | 0.0431 (1위) | 0.852 (3위) |

→ **중요도 역전 해소**: 심한 손실+큰 비중 최상단, 잡공시 최하단.

---

## 4. 미해결 설계 결정 — 크기 게이팅 (운영자 판단 필요)

pure noisy-OR는 곱셈이 주던 "크기 게이팅"을 잃는다. 딥노이드가 예: 비중 1%인데 +33% 변동(unrl=1.0)
하나로 2위(0.882). 곱셈에선 stake 0.033이 전체를 눌러 0.0064(꼴찌)였다.

"소액 포지션의 큰 %변동을 상단에 올릴 것인가?"는 트리아지 철학 결정:

- **(A) 그대로 둔다** — "+33%는 1%짜리라도 확인할 가치". 단순·투명. pure noisy-OR 유지.
- **(B) unrl/move를 포트폴리오 상대값으로 재정의** — 미실현손익을 "총자산 대비 %"로,
  변동을 "비중 가중"으로. 딥노이드의 33%×1% = 포트 0.33%라 자연 강등. 크기 게이팅을
  컴포넌트 정의에 내장(집계는 여전히 OR). 더 원칙적이나 컴포넌트 재정의 필요.

**권장: (A)로 시작**(변경 최소, 관찰 가능하게) → 실계정에서 "소액 대박/폭락이 큰 포지션을
가리는지" 관찰 후 필요시 (B). 단독 신호 지배가 목적이므로 (A)가 트리아지 취지엔 더 충실.

---

## 5. disc 하위 디테일 (§3에 흡수, 별도 튜닝)

곱셈에서 빠지면서 disc 과대부양은 이미 완화되지만, `c_disc` 자체도 정리:

- **카테고리 가중**: "기타"(미분류)·소유상황류는 0.3~0.5 배율, material(유증·CB·배당·실적·소송)은 1.0.
  현재 "기타" 12일 전 1건이 0.144나 나오는 건 카테고리 무차별 탓.
- **decay 확인**: 12일 전이 `exp(-12/7)=0.178`인데 출력 0.144 — 건수 블렌드 확인. 룩백 14 유지하되
  실효 감쇠가 잡공시 꼬리를 충분히 죽이는지 점검.

---

## 6. 재보정 (형태 변경의 필연적 후속)

점수 분포가 [0,0.05] → [0,0.96]로 바뀌므로:

- **`scoreFloor` 재설정**: 0.006은 곱셈 스케일 값. noisy-OR에선 "이 이하 신호는 조용함"의
  새 임계 필요. 초기값 제안 **0.15**(단일 약신호도 못 넘는 선). 실데이터 분포 보고 확정.
- **top-K=5 유지**. dedupe·보유 우선·관심 baseline 0.3 **불변**.

---

## 7. 불변 원칙 (변경 금지)

- R1(현저성 정렬, 방향 배제 — 크기만) · R2(이벤트 배지 O / 해석 X). 인과 미서술.
- 방향색은 가격·미실현 배지에만. 게이지/범주 무채색.
- `FORBIDDEN_ATTENTION` 전수 스윕(BASE + 호재/악재/기회/추천/목표가) 전 브랜치 통과.
- 임계값·가중치 전부 provisional 뱃지 유지 — Phase 4 백테스팅 대상.

---

## 8. 수용 기준 (acceptance)

1. **역전 해소**: 실계좌에서 컴투스홀딩스 > 유유제약 (심한 손실·큰 비중이 잡공시를 이긴다).
2. 단일 강신호(unrl=1 단독)만으로 상단 진입, 단일 약신호(disc≈0.14 단독)는 미진입.
3. 기존 테스트(순수 score 12 + facts/interpret/service) 갱신 통과 + 금지어 스윕 불변.
4. **합성 케이스 추가**: 공시 다수 종목 3~4개를 넣어 "공시 종목 상위 독점"이 재발 안 하는지
   (지금 실계좌는 공시 1건뿐이라 독점 스트레스 테스트 불가 — 합성으로 보강).

---

## 9. 남은 확인 (구현 전)

- [ ] 실제 `score.js`와 §2 컴포넌트 계산부 대조 (GitHub 401로 이번 세션 미대조).
- [ ] disc 블렌드 공식 원문 확인(건수×최신성 결합 방식) — §5 카테고리 가중 삽입 지점 파악.
- [ ] 공시 다수 계정 or 합성 픽스처로 독점 재발 여부 최종 관찰.
